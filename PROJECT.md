# PROJECT.md — Number Sequence Bot (บอทเรียงเลข)

Single source of truth. อ่านไฟล์นี้ก่อนแก้อะไร.

## คืออะไร

บอทเดสก์ท็อป Windows (PySide6) ที่แก้เกม **"ตารางเลข" (Schulte grid)** อัตโนมัติ — เกมมีตัวเลข
1–25 สลับตำแหน่งในตาราง 5×5 ต้องแตะเรียงจากน้อยไปมาก เกมรันอยู่ใน **LDPlayer** (Android emulator
บน PC) เหมือนวิธีที่โปรเจกต์พี่น้อง `D:\Github\bot_detect_word` ใช้กับฟีเจอร์ "จับคู่การ์ด"

Flow: อ่านค่า **"เลขต่อไป"** ด้วย OCR → capture ตารางทั้งก้อน หั่นเป็น 25 ช่อง OCR หาตำแหน่งเลขนั้น
→ วาดวงไฮไลต์เขียวชี้เป้า → คลิกเมาส์อัตโนมัติ (ctypes) → วนซ้ำจนครบ `max_number` เลข

ต้นทางของงาน: ผู้ใช้ส่งสกรีนช็อตเกม (Thai UI, สนาม "เวลาที่ใช้"/"เลขต่อไป") แล้วขอให้สร้างบอทมา
เขียนไว้ใน `workspace/admin/Hiu` นี้ — ผู้ใช้ขอให้ "ทำเร็วๆ พร้อมรัน" จึงข้ามขั้น mockup UI (ดู
`plan/_TEMPLATE.html` ที่ยังเหลือไว้เผื่อทำเอกสารแผนอื่นในอนาคต — 3 mockup ตัวเลือก UI ที่เคยสร้าง
ถูกลบไปแล้วเพราะไม่ได้ใช้)

## Stack

- GUI: **PySide6** (Qt) — หน้าตาแบบ `bot_detect_word` (ผู้ใช้ขอ "สวยๆ แบบ WORD_DETECT_BOT"): `theme.py`
  (design token + QSS + ฟอนต์ + ไอคอนย้อมสี ยกจาก `bot_detect_word/ui_theme.py` ธีมมืด) + `widgets.py`
  (Section, StatDot, Segmented, Stat, setting_row) — ฟอนต์ **Anuphan + JetBrains Mono** ใน `fonts/` (OFL),
  ไอคอน **Lucide** ใน `assets/icons/` (ISC, มี LICENSE). ห้ามมีสี hex ลอยนอก `theme.PALETTE`.
  (2026-09-23 ผู้ใช้ลองเปลี่ยนเป็น TH Sarabun New แล้วขอกลับเป็นฟอนต์เดิม — ถ้าจะเปลี่ยนอีก: Sarabun ตัวเล็กกว่า
  Anuphan ~1.33 เท่าที่ px เท่ากัน ต้องขยายขนาดทุกจุด และสตริงไทยใน `mono_font(mix=True)` จะตัวจิ๋ว)
- Capture: **mss** (สร้าง instance ใหม่ทุก `grab()` — thread-safe)
- OCR: **pytesseract** → Tesseract ที่ `C:\Program Files\Tesseract-OCR\tesseract.exe` (auto-detect
  ใน `capture.configure_tesseract`, override ได้ผ่าน `Settings.tesseract_cmd`)
- คลิก (`mouse.py`, ไม่มี dependency เพิ่ม): จุดที่อยู่บนหน้าต่าง **scrcpy** (class `SDL_app`) → แตะตรงเข้า
  มือถือผ่าน scrcpy-server ตัวที่สอง (`phonetap.py`) ไม่ขยับเมาส์จริง, สำรองด้วย `PostMessage`; อย่างอื่น
  (Chrome/LDPlayer) → `SendInput`
- Panic key: ไลบรารี `keyboard` (global hotkey, default `esc`) — ต้องรัน PowerShell แบบ
  Administrator บางเครื่องถึงจะดักคีย์ข้ามหน้าต่างได้ (เหมือน `bot_detect_word`)

## เทคนิคสำคัญที่ต้องรู้ก่อนแก้

### Overlay คลิกทะลุกลางกรอบ (`overlay.py`)
ใช้ `setMask(บริเวณทั้งหมด − รูตรงกลาง)` **ห้ามใช้ `Qt.WindowTransparentForInput`** — ทำให้คลิก
ทะลุทั้งบานจนลากขอบ/ย่อขยายไม่ได้เลย (บทเรียนที่บันทึกไว้ใน `bot_detect_word/PROJECT.md` แล้ว —
ยึดตามนั้น). ผลคือเฉพาะเส้นขอบหนา (`_BORDER = 6px`) รับคลิกได้ (ลาก/ย่อขยาย) ส่วนตรงกลางคลิกทะลุ
ไปโดนหน้าต่าง LDPlayer ข้างล่างแทน.

`FrameOverlay` มี callback `on_change(rect)` เรียกทุกครั้งที่ขยับ/ย่อขยาย/ปล่อยเมาส์ — `app.py`
ใช้มันอัปเดต `settings.grid_box` / `settings.next_box` แบบ live ไม่ต้องกดปุ่ม "บันทึกตำแหน่ง" แยก.

### พิกัด logical vs physical (`screenmap.py`) — สำคัญมาก
Qt 6 ใช้ **logical px** (กรอบ overlay, `config.json` เก็บค่านี้) แต่ **mss และ `SendInput` ใช้ physical
px** บนจอที่ scale ≠ 100% (เครื่องผู้ใช้: 2560×1600 @ 125%) ถ้าส่งพิกัด Qt ไปจับภาพตรงๆ จะจับผิดที่ทั้ง
ก้อน — เคยพังแล้ว: บอทขึ้น "อ่านเลขต่อไปไม่ออก" รัวๆ เพราะไปจับเมนูของหน้าต่างอื่น. `Solver.start()`
(GUI thread) สร้าง `ScreenMap.for_rect()` จากจอที่กรอบอยู่ แล้วลูปใช้ `to_physical()` ก่อน
`capture.grab`/`read_grid_numbers` ทุกครั้ง; ผลจาก OCR เป็น physical → คลิกตรงๆ ได้ ส่วนวงไฮไลต์
(หน้าต่าง Qt) แปลงกลับด้วย `point_to_logical()`. **ห้ามส่งพิกัดจาก settings เข้า capture/mouse ตรงๆ**

### แตะมือถือผ่าน scrcpy-server ตัวที่สอง (`phonetap.py`) — ทางคลิกหลักเมื่อเล่นผ่าน scrcpy
เกมเล่นบนมือถือจริง (vivo V29, แอป Shopee) ผ่านหน้าต่าง **scrcpy 4.1 (SDL3)** บนเดสก์ท็อป 5120×1440 @100%.
ที่มา (วัดด้วย `dumpsys input`/Pointer location ของมือถือ, 2026-09-22):
1. `SendInput` **ไม่ถึงมือถือเลยสักครั้ง** ทุกรูปแบบ (กดสั้น/ค้าง 100ms, scrcpy active หรือไม่) — คลิก title bar
   หน้าต่างอื่นก็ไม่ทำให้มัน active คือ SendInput ในเครื่องนี้ไม่ถึงหน้าต่างไหนเลย (ยังไม่รู้ต้นเหตุชั้นลึก).
   ตอนสั่งผ่าน Chrome Remote Desktop ใช้ได้เพราะ input เข้ามาทาง `remoting_host` คนละทาง
2. `PostMessage` เข้าหน้าต่าง scrcpy ถึง แต่**พลาดเป็นช่วงๆ**: SDL เอาตำแหน่งกด/ปล่อยจาก move ล่าสุด และ move
   ทำให้ Windows ส่ง `WM_MOUSELEAVE` (เมาส์จริงไม่อยู่บนหน้าต่าง) ซึ่ง SDL ย้ายตำแหน่งไปที่เมาส์จริง — เว้น 10ms
   ระหว่าง move→down แตะไปโดนมุมขวาบนจอมือถือ; โพสต์ติดกันผ่านตอนเทสต์ครั้งเดียว แต่เล่นจริงยังแพ้ race (SDL
   เป็นอีก process อาจประมวล move ก่อนเราโพสต์ down ทัน): log จริง เกมไม่รับแตะช่องเดิม 2 รอบติด แม้เป็นแตะแรก
   หลังหยุดรอ 1.5s → เหลือเป็นแค่ทางสำรอง (`mouse._post_click`)
3. **ทางหลัก**: เปิด scrcpy-server อีกตัวบนมือถือ (scid ของตัวเอง, `video=false audio=false control=true
   cleanup=true power_on=false clipboard_autosync=false`, jar ที่ `/data/local/tmp/hiu-bot-scrcpy-server.jar`)
   แล้วส่ง `INJECT_TOUCH_EVENT` (pointer GENERIC_FINGER) ด้วยพิกัดมือถือตรงๆ — screen size ในข้อความ = ขนาดจอจาก
   `wm size` (สลับถ้าหน้าต่างแนวนอน) ใช้ได้แม้ไม่มีวิดีโอ. E2E: `mouse.click` ตัวจริง 10/10 ตรงจุด (±2px),
   31ms/แตะ, ต่อ ~0.75s, scrcpy ตัวจริงของผู้ใช้ทำงานต่อปกติ, ปิดแล้ว jar/forward ไม่ค้าง
- **adb.exe + scrcpy-server + เวอร์ชัน ต้องมาจากโฟลเดอร์ของ scrcpy.exe ที่เป็นเจ้าของหน้าต่างเสมอ** (หาจาก pid
  ของหน้าต่าง `SDL_app`, เวอร์ชันจาก `scrcpy.exe --version`) — adb คนละเวอร์ชันจะ kill adb server ของ scrcpy
  ตัวจริง (เคยเจอกับ AirDroid Cast: "adb server version (40) doesn't match this client (41); killing...")
- เลือกเครื่อง: `adb devices -l` เครื่องเดียวใช้เลย, หลายเครื่องจับ `model:` กับชื่อหน้าต่าง scrcpy
- แปลงจุดบนจอ → สัดส่วนบนจอมือถือ (`mouse._frame_fraction`) หักขอบดำที่ scrcpy เติมเมื่อสัดส่วนหน้าต่างไม่ตรง
- `Solver._run` เรียก `mouse.prepare()` ก่อนเริ่ม (ต่อมือถือ ~0.75s ไม่ให้กินเวลาแตะแรก) แล้ว log ว่าใช้ทางไหน;
  ต่อไม่ได้ → จำความล้มเหลวไว้ (`_phone_failed`) ไม่ลองต่อใหม่ทุกแตะ ใช้ PostMessage จนกว่าเริ่มรอบใหม่
- หาหน้าต่างเป้าด้วย `EnumWindows` เฉพาะ class `SDL_app` ที่คลุมจุดนั้น (ไม่ใช้ `WindowFromPoint`) จึงไม่โดนกรอบ
  overlay/วงไฮไลต์ของบอทหรือหน้าต่างอื่นที่ทับอยู่. ctypes ตั้ง `argtypes` บน `WinDLL("user32")` แยกของตัวเอง
  — ถ้าไม่ตั้ง HWND/LPARAM บน x64 จะโดนตัดเหลือ 32 บิต

### หน้าต่างหลัก (`app.py`) — สถานะต้องเห็นชัด
ผู้ใช้บ่น "กดเริ่มแล้วไม่รู้ว่ารันหรือหยุด" (ปุ่มเริ่ม/หยุดเดิมหน้าตาเหมือนเดิมทั้งตอนใช้ได้และไม่ได้) → ตามแบบ
`bot_detect_word`: เซ็กชัน **ควบคุม** บนสุดมี `StatDot` บรรทัดใหญ่ (ว่าง / กำลังเริ่ม… จุดเหลืองเต้น / **กำลัง
ทำงาน จุดเขียวเต้น** / เสร็จแล้ว ✓ / หยุดอยู่ ■ / หยุดเพราะมีปัญหา ✗ แดง + ข้อความ error ล่าสุด) + บรรทัดรายละเอียด
(เลขที่กำลังกด · เวลา) + **ปุ่มเดียวสลับ** ▷ เริ่มอัตโนมัติ (ขาว) ↔ ■ หยุด + ชื่อหน้าต่างขึ้น "▶ กำลังทำงาน —"
ระหว่างรัน. ตัวเลขใหญ่ mono: เลขต่อไป / กดแล้ว / เวลา. ระหว่างรันล็อกปุ่มโหมดและไม่ดึงค่าจาก spinbox เข้า settings
(ลูปกำลังใช้อยู่). เรนเดอร์ดูหน้าตาจริงได้โดยไม่ต้องเปิดหน้าต่าง: สร้าง `MainWindow` บน platform ปกติแล้ว `grab()`
(โหมด offscreen ไม่มีฟอนต์ไทย ตัวหนังสือเป็นกล่อง)

### กดเริ่มแล้วแตะทันที — standby (`Solver.start_standby`)
เดิมกดเริ่มแล้วรอ ~1.3s (ต่อมือถือ ~0.75s + อ่านทั้งตาราง ~0.5s). ตอนว่าง thread standby จับภาพตารางทุก 0.15s
(ถูก) → ภาพ**นิ่งครบ 0.3s** และยังไม่เคยลองอ่าน → ต่อมือถือรอไว้ + `_sync(quiet=True)` เก็บเป็น `_Prepared`
(key = โหมด/แถว/คอลัมน์/เลขสูงสุด/กรอบ). กดเริ่ม → `_take_prepared` (รอ standby ที่กำลังอ่านอยู่ผ่าน
`_prepare_lock`) จับภาพใหม่ถ้ายัง `grid_similar` กับตอนอ่าน → แตะเลขแรกทันที ไม่งั้นอ่านใหม่ตามปกติ. UI โชว์
"⚡ อ่านตารางไว้แล้ว — กดเริ่มได้ทันที" ใต้ปุ่ม. **จำภาพที่ลองอ่านแล้ว (`_sb_tried`) ทั้งที่อ่านออกและไม่ออก** —
ไม่งั้นภาพนิ่งที่อ่านไม่ออก (เกมอื่นเต็มจอทับ) โดน OCR ซ้ำทุก 0.3s (วัดจริงตอน Valorant เต็มจอ: 2 ครั้งใน 3s →
หลังแก้ 1 ครั้งใน 5s). `capture.grid_similar` เทียบ**ทีละช่อง**: พิกเซลต่าง > 40 (หลังเบลอ 1px) เกิน 1% ของช่อง
= เปลี่ยน — ภาพ scrcpy จริง: noise JPEG q30 = 0.0%, เลขเปลี่ยนจริง ≥ 3.8% (แม้ 28→29). ห้ามย่อภาพก่อนเทียบ
(ย่อเหลือช่องละ 12px แล้ว 7→10 ต่างเฉลี่ยแค่ 3.4 จับไม่ได้). เทสต์ UI ปิด standby (`Solver.start_standby` no-op)

### 2 โหมด (`config.mode`, เลือกใน segmented control เซ็กชัน "โหมด")
- **`ocr`** (ค่าเริ่มต้น) — 2 กรอบ: ตาราง + เลขต่อไป. ยิงล่วงหน้าแล้วยืนยันจากกรอบเลขต่อไป แตะไม่ติดก็ resync เอง
- **`grid`** — กรอบตารางอย่างเดียว (`Solver._solve_grid`): `_sync(use_next_box=False)` ครั้งเดียว (เลขถัดไป =
  เลขน้อยสุดที่สอดคล้องกับตาราง ผ่าน gridsolve) แล้วกดเรียงจนครบตามจังหวะด้วยตำแหน่งที่ทำนาย **ไม่มีการยืนยัน
  จากเกม** — แตะไหนไม่ติดจะไม่รู้ตัว (ผู้ใช้เลือกแบบนี้เอง: ง่าย/เร็ว). UI ซ่อนกรอบเลขต่อไป + ปิดปุ่มของมัน

### OCR เลข (`capture.py`)
Pipeline ต่อภาพ: autocontrast → **`_crop_to_digits`** (connected components ของพิกเซลหมึก ≤150, ทิ้ง
ชิ้นที่แตะขอบภาพ = เส้นขอบ overlay ที่ mss จับติดมา/ขอบการ์ด/เศษเซลล์ข้างๆ, เก็บเฉพาะชิ้นที่สูง ≥60%
ของชิ้นสูงสุดในบรรทัดเดียวกัน = ทิ้งป้ายไทย "เลขต่อไป", เผื่อขอบ 30% ของความสูง) → **ปรับความสูงตัวเลข
ให้คงที่** → threshold → `MedianFilter(3)` → เติมขอบขาว 20px

**ห้ามกลับไปอัปสเกลตายตัว (เดิม 4x)** — ฟอนต์หนาของเกมจริงจะสูง ~150px จน Tesseract อ่าน "8" ไม่ออก
และอ่านผิดแบบมั่นใจ (25→29, 27→2). วัดกับ 576 ภาพ (เกมจริง + Arial/Arial Black/ส้ม 22–56px) และ
Arial Black 22–48px × เลข 1–50 (1,350 ภาพ): pipeline ใหม่ถูกหมด 0 ผิด, ของเดิมผิด 10/176

**`read_number_robust(img, valid_max)`** ลองตามลำดับ: `psm 6` @ 40px → 32px → 52px → `psm 7` @ 40px
→ `psm 8` @ 40px; ยอมรับค่าแรกที่อยู่ใน `1..valid_max`; ไม่มีเลย → คืนค่าแรกที่ไม่ใช่ None.
เหตุผล: `psm 8` อ่าน **`"1"` → `"4"`, `"11"` → `"411"`** แบบมั่นใจผิด (อันตราย — ตำแหน่งเลข 1 ทับเลข
4 จนคลิกผิด) จึงอยู่ท้ายสุด; `psm 6` พลาดแบบ None (ปลอดภัย) แต่อ่าน "8" ตัวเดียวฟอนต์หนาไม่ออกทุกขนาด
→ `psm 7` รับช่วง; Tesseract สะดุดบางขนาดแบบสุ่ม → ลองหลายความสูง. **แก้ลำดับนี้ต้องรัน
`tests/test_ocr.py` + `test_real_capture.py` + sweep หลายขนาดฟอนต์ก่อนเสมอ** — ค่าที่ดูดีกับภาพเดียว
มักพังที่ขนาดอื่น (เจอมาแล้วระหว่างจูน)

### ตรวจทานผล OCR ด้วยกติกาเกม (`gridsolve.py`)
OCR ทีละช่องแม่นมากแล้ว (ภาพ scrcpy จริง 25/25, ย่อ/JPEG 12 แบบผิด 1 ช่อง) แต่ที่ผิดคือ**ผิดแบบมั่นใจ**
(วัดจริง: ย่อ 0.65 + JPEG 60 อ่าน 15 เป็น 18) ซึ่งทำให้กดผิดช่องทันที. กติกา: ตารางมีเลข next ..
next + rows×cols − 1 (ไม่เกิน max) ครบทุกตัว ตัวละช่อง ที่เหลือว่าง — OCR แค่ต้องแยกเลขในชุดที่รู้อยู่แล้ว.
`Solver._sync` (ทุกครั้งที่อ่านทั้งตาราง): อ่านรอบแรกแบบเดิม → `gridsolve.solve` ให้คะแนนทุกคู่ (ช่อง, เลข)
(ตรง = 10 ต่อรอบ OCR, หลักตรงตำแหน่ง = 1, อ่านไม่ออกคู่กับช่องว่าง = 4) แล้ว Hungarian เลือกการจับคู่ที่คะแนน
รวมสูงสุด → ช่องที่ได้เลขแบบไม่มีเสียงข้างมากรองรับ + ช่องที่อ่านได้เลขเดียวกับมัน (`conflicts`) อ่านซ้ำครบทุกรอบ
ใน `_ATTEMPTS` (`capture.read_cells_votes` ยิงทุก (ช่อง, รอบ) พร้อมกัน) แล้วแก้ใหม่. เลขต่อไปลองทั้งค่าจากกรอบ
เลขต่อไปและเลขน้อยสุดในตาราง เลือกอันที่คะแนนรวมสูงกว่า — กรอบเลขต่อไปอ่านผิด/อ่านไม่ออกจึงถูกตารางแก้.
เลขซ้ำจากรอบเดียวกำกวมจริง (สองช่องอ่าน 29 — ไม่รู้ว่าช่องไหนตัวจริง) ต้องพึ่งการอ่านซ้ำเสมอ.
ต้นทุน: ภาพปกติ ~0.51s ต่อการ sync, มีช่องขัดกัน ~0.72s (เกิดตอนเริ่ม/resync เท่านั้น ไม่ใช่ทุกคลิก)

`cell_pad_ratio` default **0.16** — inset แต่ละเซลล์ 16% ต่อด้านก่อนตัดภาพ กันขอบข้างเซลล์เพื่อน
บ้านหลุดเข้ามาปน (ยิ่งตารางไม่มีช่องว่างชัดระหว่างการ์ด ยิ่งต้องการ pad สูง).

### Solver loop (`solver.py`)
รันบน `threading.Thread` แยก ไม่ใช่ QThread — สื่อสารกลับ UI ผ่าน **Qt Signal** (`log`,
`target_found`, `progress`, `finished`) ซึ่ง Qt จะคิวข้าม thread ให้เองอัตโนมัติ (ปลอดภัย ไม่ต้อง
`QMetaObject.invokeMethod` เอง). Safety: หยุดเองถ้าหาเลขเป้าหมายไม่เจอในตารางติดกันเกิน
`settings.max_consecutive_miss` (default 20) รอบ — กันวนเปล่าๆ ไม่จบตอน OCR อ่านพังต่อเนื่อง.

**ความเร็ว (เป้าผู้ใช้ล่าสุด: 50 เลขใน 8 วินาที = 163ms/คลิก)** — OCR หนึ่งครั้ง ~77ms (เกือบทั้งหมดคือค่า
เปิด process tesseract), grab หนึ่งครั้ง ~17ms (1 เฟรม vsync) และจอ scrcpy ตามหลังมือถือ ~0.1–0.2s.
แบบแรกอ่าน 25 ช่องใหม่ทุกคลิก + นอนตายตัว 350ms = ~2.5s/คลิก; แบบที่สองรอจอยืนยันทีละคลิก
(`_wait_for_advance`) = ความหน่วงจอ + OCR ต่อคลิก ~0.22–0.3s ไม่ทัน 8 วินาที. ตอนนี้:
- **อ่านทั้งตารางครั้งเดียวแล้วทำนายต่อ** — เกมไม่สลับตำแหน่ง กดเลข n แล้วช่องนั้นกลายเป็น n+25 ที่เดิม
  (ยืนยันจากสกรีนช็อต 2 ภาพ) `where[n + rows×cols] = ช่องของ n` ไม่ต้อง OCR ช่องซ้ำระหว่างเล่น
- **ยิงล่วงหน้า ยืนยันตามหลัง** — `_NextWatcher` (thread แยก) grab กรอบเลขต่อไปถี่ๆ OCR เฉพาะตอนภาพเปลี่ยน
  ลูปหลักคลิกตามนัดได้เลย ขอแค่คลิกที่จอยังไม่ยืนยันค้างไม่เกิน `_MAX_IN_FLIGHT` = 3 (ที่ 163ms/คลิก รับ
  ความหน่วงรวมได้ ~0.49s). เชื่อเลขจากจอเฉพาะช่วง confirmed+1..target จึงไม่ถอยหลังเพราะ OCR อ่านผิด.
  **เลขสุดท้ายรอจอยืนยันครบก่อน** (limit 1) แล้วจบเมื่อภาพหลังคลิกไม่ใช่เลขสุดท้าย
- คลิกเก่าสุดที่ยังไม่ยืนยันค้างเกิน `_CONFIRM_TIMEOUT_S` 1.5s → อ่านเลขต่อไป + ทั้งตารางใหม่ (self-healing);
  ราคาของการยิงล่วงหน้า: ถ้าเกมไม่รับคลิกไหน คลิกที่ตามไปก่อนจับได้โดนเลขผิดไม่เกิน 2 ครั้ง
- OCR ขนานใน `capture._ocr_pool` (12 worker, `OMP_THREAD_LIMIT=1`): อ่านทั้งตาราง 1.9s → 0.3s และ
  กรอบเลขต่อไปใช้ `read_number_robust(parallel=True)` ยิงทุกรอบ fallback พร้อมกัน
- `tests/test_solver.py` จำลองจอหน่วง 150ms: แบบรอทีละคลิกใช้ 7.47s, แบบใหม่จบตามเป้า 4s
- ถ้าเกมเวอร์ชันอื่นสลับตำแหน่งหลังกด ระบบยังถูก (คลิกไม่ติด → resync) แต่จะช้าลงมาก
- **"เวลาเป้าหมาย (วินาที)"** (`target_total_s`, 0 = เร็วสุด) — ผู้ใช้อยากให้จบ ~18s ไม่ใช่เร็วสุด
  คลิกที่ k นัดไว้ที่ คลิกแรก + k × T/(max_number−1) (นับจากนัด ไม่ใช่นอนเท่ากันทุกคลิก จึงไม่เพี้ยนตาม
  ความเร็ว OCR/เกม) — `click_delay_ms` ตั้งแบบนี้แทนไม่ได้เพราะเวลาตอบสนองของเกมไม่แน่นอน

## โครงสร้างโค้ด

ดูตาราง "โครงสร้างโค้ด" ใน `README.md` — ไม่ซ้ำไว้ที่นี่สองที่.

## รันและทดสอบ

```powershell
.venv\Scripts\python.exe main.py                              # รันแอป
.venv\Scripts\python.exe tests\test_ocr.py                    # OCR เลขเดี่ยว
.venv\Scripts\python.exe tests\test_grid.py                   # OCR ตาราง 5x5 เต็ม (สุ่มทุกรัน)
.venv\Scripts\python.exe tests\test_real_capture.py           # ภาพเกมจริง (Chrome 125% + scrcpy) + _sync กับภาพย่อ/JPEG
.venv\Scripts\python.exe tests\test_gridsolve.py              # จับคู่ช่อง↔เลขด้วยกติกาเกม + Hungarian เทียบ brute force
.venv\Scripts\python.exe tests\test_solver.py                 # ลูป solver กับเกมจำลอง (ลำดับ + อ่านตารางครั้งเดียว)
.venv\Scripts\python.exe tests\test_screenmap.py              # แปลงพิกัด logical <-> physical
.venv\Scripts\python.exe tests\test_mouse.py                  # ทางคลิก phonetap/PostMessage (หน้าต่าง Win32 นอกจอ) / SendInput
.venv\Scripts\python.exe tests\test_phonetap.py               # parse adb + byte ข้อความแตะ + server ปลอม (ไม่ต้องมีมือถือ)
$env:QT_QPA_PLATFORM="offscreen"; .venv\Scripts\python.exe tests\test_app_smoke.py  # สร้างหน้าต่าง headless
```

ทั้ง 9 เทสต์เขียวหมดก่อนแก้อะไรเสร็จเสมอ (ดู `test_app_smoke.py` ที่ no-op `Settings.save`
ป้องกัน `config.json` จริงโดนทับ — อย่าลบบรรทัดนั้น).

## ยังไม่ได้ทำ / ข้อจำกัดที่รู้อยู่

- เกมจริงที่ผู้ใช้เล่นตอนนี้อยู่ใน **Chrome (shopee arcade)** หรือบน**มือถือจริงผ่าน scrcpy** ไม่ใช่ LDPlayer — มีภาพจริงชุดเดียวใน
  `tests/fixtures/` (ตัวเลข 1–50 บนตาราง 5×5, `max_number` = 50) ถ้าเกมเปลี่ยนสี/พื้นเข้ม ต้องปรับ
  `_INK_THRESHOLD`
- บอทยังต้องเห็นหน้าต่าง scrcpy บนจอเพื่อจับภาพ (ห้ามมีอะไรบัง) — แตะไม่ต้องเห็น แต่อ่านเลขต้องเห็น
- `phonetap` ผูกกับรูปแบบข้อความของ scrcpy 4.x — อัปเดต scrcpy เวอร์ชันหลักใหม่ต้องเทียบ `INJECT_TOUCH_EVENT`
  กับซอร์สใหม่ (ถ้าไม่ตรง server จะปิด/ไม่รับ → ตกไปทางสำรอง PostMessage ที่พลาดเป็นช่วงๆ)
- โหมด `grid` ไม่ยืนยันจากเกม — แตะไหนไม่ติดจะกดเลขถัดๆ ไปผิดทั้งหมดโดยไม่รู้ตัว
- `mouse._abs_coords` ใช้ขนาดจอหลักเท่านั้น — กรอบบนจอที่สองจะคลิกผิด (ยังไม่รองรับ virtual desktop)
- ยังไม่มี build เป็น `.exe` portable (ผู้ใช้ขอ "เร็วที่สุด" จึงข้ามไปก่อน รันจาก `.venv` เท่านั้น)
- ยังไม่มี auto-detect ตำแหน่งหน้าต่าง LDPlayer (ผู้ใช้ลากกรอบเองด้วยมือทุกครั้งที่เกมขยับ)
- ไม่มีระบบ export/import ค่า config ข้ามเครื่อง (ต่างจาก `bot_detect_word` ที่มีปุ่ม Export)

## ประวัติการแก้ไข

- **2026-09-22** — สร้างโปรเจกต์ครั้งแรก: overlay 2 กรอบ (ตาราง/เลขต่อไป) + OCR 2-pass (psm 6→8
  + valid_max guard) + solver loop + ctypes click + panic key + เทสต์ headless 3 ตัว
- **2026-09-22** — แก้ "อ่านเลขต่อไปไม่ออก" กับเกมจริง: (1) แปลงพิกัด logical→physical (`screenmap.py`)
  เพราะจอ 125% ทำให้จับภาพผิดที่ (2) OCR ตัดเหลือตัวเลข + ปรับความสูงคงที่ + ลำดับ psm 6→7→8 หลายขนาด
  (3) Ctrl+C ในเทอร์มินัลปิดแอปปกติ (เดิม KeyboardInterrupt traceback ใน `_tick_elapsed`) — `main.py`
  (4) เพิ่ม `tests/test_real_capture.py` (+fixtures) และ `tests/test_screenmap.py`
- **2026-09-22** — เร่งความเร็ว ~2.5s → ~0.16s ต่อคลิก: cache ตำแหน่งเลข + ยืนยันคลิกจากกรอบเลขต่อไป
  แทนการนอนรอ, OCR ขนาน, ลบ `cooldown_after_click_ms` (350ms ที่ซ่อนอยู่ ไม่มีใน UI), mouse settle
  30→10ms + `tests/test_solver.py`
- **2026-09-22** — แก้ "กดเลขแล้วเกมไม่ขยับ" ตอนเล่นผ่าน scrcpy: คลิกลงหน้าต่าง scrcpy ด้วย `PostMessage`
  (move ติดกับ down/up) แทน `SendInput` ที่ไม่ถึงมือถือเลย + `tests/test_mouse.py`
- **2026-09-22** — เป้า 50 เลขใน 8 วินาที: solver ยิงล่วงหน้า (`_NextWatcher` + `_MAX_IN_FLIGHT` 3) แทนรอจอ
  ยืนยันทีละคลิก และทำนายตำแหน่งเลข 26–50 แทน OCR ช่องซ้ำ + เทสต์จอหน่วงใน `tests/test_solver.py`
- **2026-09-22** — ความแม่น: `gridsolve.py` ตรวจทานผล OCR ทั้งตารางด้วยกติกาเกม (Hungarian + อ่านซ้ำช่องที่
  ขัดกัน) แทน `_read_positions`/`_read_next_target` ตอน sync + fixtures ภาพ scrcpy จริง + `tests/test_gridsolve.py`
- **2026-09-22** — แก้ "กดแล้วพลาดบ่อย" (PostMessage แพ้ race ใน SDL): `phonetap.py` แตะตรงเข้ามือถือผ่าน
  scrcpy-server ตัวที่สอง (E2E 10/10) + `mouse.prepare()` + 2 โหมด (`ocr` / `grid` ตารางอย่างเดียว) ใน UI +
  `tests/test_phonetap.py`
- **2026-09-22** — กดเริ่มแล้วแตะทันที (standby อ่านตารางล่วงหน้า + `capture.grid_similar`) + UI ใหม่ทั้งหน้าแบบ
  `bot_detect_word` (`theme.py`, `widgets.py`, ฟอนต์/ไอคอน) + สถานะรัน/หยุดชัดเจน (StatDot + ปุ่มสลับเดียว) +
  เทสต์ standby / `grid_similar` (ภาพ scrcpy จริง) / สถานะใน UI
