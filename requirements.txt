PySide6>=6.7
mss>=9.0
pytesseract>=0.3.10
Pillow>=10.0

# ปุ่มหยุดฉุกเฉิน (hotkey.py) — คนละไลบรารีกันตามระบบ
# Windows: ไลบรารี keyboard. บน macOS ตัวนี้บังคับรันด้วย root และลาก pyobjc มาทั้งชุด จึงไม่ลงเลย
keyboard>=0.13.5; sys_platform == "win32"
# macOS: เรียก Quartz/AppKit ตรงๆ — ลงเฉพาะ 3 ตัวที่ใช้จริง (Quartz = จับหน้าต่าง/คลิก/ดักปุ่ม,
# Cocoa = AppKit วัดแถบหัวหน้าต่าง, ApplicationServices = เช็คสิทธิ์ Accessibility)
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=10.0; sys_platform == "darwin"
